# FINDINGS 2026-07-11 -- #054 oriented coincidence bell + chiral binary code: the #052 asymmetry law repurposed from READER to TRIGGER/WRITER

## What
Two demonstrations on graved objects (#044 calendar, #052 asymmetry law):
(1) ORIENTED BELL: at the calendar rendezvous (deep relief minimum), the
signed heterodyne difference D(u) fires with a SIGN identifying the
orientation -- one-way choir +1, its mirror -1, symmetric choir mute
(no orientation, module-trapped wall as predicted).
(2) CHIRAL BINARY CODE: a 32-bit message ('EML*') encoded as a sequence
of oriented choirs (bit = winding sense) and decoded from INTENSITY-ONLY
heterodyne data by the full-window residual vote (#052 panel-D
discriminator): BER 0/32 at 0%, 1% and 5% multiplicative noise.

## Results (Anthony's machine, 2026-07-11, verdict 21/21)
- Bell signs +1/-1 exact, |D| = 0.130-0.131, stable across 2 choir grids
  (midpoint/endpoint) x 2 window samplings -- the #049 grid-change control
  passes; symmetric-choir residue 7.4e-05 (midpoint) / 5.0e-03 (endpoint,
  the known #049 first-order discreteness).
- J3 numeric: max |D + D_mirror| = 0.00e+00 (exact pointwise antisymmetry).
- Binary code: 0/32 errors at all three noise levels (matched-template
  residual vote; single-point sign decoding is fragile at sigma*r -- the
  #052 noise lesson reconfirmed, remedy identical).
- Controls: reciprocal (all nu=0) REFUSED (flat relief, contrast 0);
  shuffle rings with its own measure's sign (pairing matters).

## Judge (SymPy, exact)
J3: the mirror map M[mu] = e^{i(a+b)u} conj(mu) is an exact INVOLUTION
(M[M[mu]] = mu, residue 0), whence bell antisymmetry D_mir = -D
identically. CERTIFIED (judge_bell_054.py).

## Framing and scale (explicit)
[ESTABLISHED machine + judge] for the MECHANISM only. NO NOVELTY CLAIM:
the mandatory prior-art gate is PENDING and named -- race logic
(inhibit gate / tropical algebra), SFQ asynchronous AND, Sagnac-type
nonreciprocity. This does not compete with BPSK; its niche is a bit
carried by the CHIRALITY of a one-way structure, readable from
intensity-only data. Born from the rhythm inquiry (2026-07-11 deep
research): the only surviving germ of both rhythm tracks loops back
into our own instrument -- the reader learns to write.

## Traces
kernel_oriented_bell_test.py -- judge_bell_054.py --
bell_machine_run.log (local) -- this file.

## ADDENDUM 2026-07-11 (same day, post-graving) -- gate CLOSED by external audit
The prior-art gate flagged PENDING above has been closed the same day by
the Cowork instance's independent audit (AUDIT_20260711_oriented_bell.md,
committed alongside): race logic (UCSB ISCA'14) and RSFQ pulse logic are
generic timing-decision walls; the CLOSEST NEIGHBOR is the SAGNAC
interferometer / laser gyroscope (orientation read from counter-propagating
waves; cite prominently in any write-up). Narrowed sole "apparently new"
candidate after the gate: orientation read from MODULUS-ONLY relief data
plus a known reference note, in LOGARITHMIC scale, from a SELF-GENERATED
(hierarchy-free) chiral rendezvous -- where Sagnac requires phase-sensitive
readout and an external rotation. Auditor's scale position, adopted:
#054 is a COROLLARY-APPLICATION of #052 (J3 is a hand-provable corollary;
the code is #052's discriminator used as a channel), instrument-scale.
The audit also constitutes a THIRD independent reproduction of all four
panels (J3 residue 3.55e-15, BER 0/32 at all noise levels).
Numbering note: #053 remains RESERVED for the staircase-tears campaign
(framing delivered, harness not yet built); the #052 -> #054 sequence gap
is intentional and traced here.
