# FINDINGS 2026-05-23 — Session audit (7-AI consultation + on-disk verification)

Author: Anthony Monnerot. All claims tagged [ESTABLISHED]/[DERIVATION]/[CONJECTURE]/[HEURISTIC].
Arbiter is real execution on Anthony's machine + the SymPy judge, never AI consensus or PySR markers.

## 1. Toolbox leakage — CONFIRMED [ESTABLISHED by execution]
- Phase-1 run (eml_star_decomposition.py, 2026-05-19) with my_conj FORBIDDEN reached
  MSE 4.223e-31 with a formula built from my_real + exp + log
  (complexity 17, see eml_star_decomp_log_phase1.txt).
- Mechanism: my_real(z) = (z+conj(z))/2 and my_imag(z) = (z-conj(z))/(2i) span conjugation
  (conj(z) = my_real(z) - i*my_imag(z)). Conjugation does NOT "emerge"; it is supplied.
- DECISION: any discovery/emergence test MUST exclude my_real, my_imag, my_abs2, my_conj
  from the PySR search space. Conjugation may enter only through eml*.

## 2. Judge verdict label — OVER-CLAIMS [ESTABLISHED by code reading]
- verify_exact.py (~lines 128-130): dfdzbar = simplify(diff(expr, zbar)); is_holo = (dfdzbar==0);
  verdict = "holomorphic" if is_holo else "anti-holomorphic".
- Testing dfdzbar==0 is CORRECT for holomorphy. But labelling everything else "anti-holomorphic"
  is wrong: dfdzbar!=0 proves NON-holomorphic only. True anti-holomorphic requires df/dz==0.
  Example: f=z+conj(z) is MIXED, judge would call it "anti".
- DECISION [TO FIX]: rename verdict to "non-holomorphic", OR add a df/dz==0 test to separate
  pure-anti from mixed. Do not write "detected anti-holomorphic" in any paper based on current judge.

## 3. eml* definition — CORRECT [ESTABLISHED by code]
- verify_exact.py op_emlstar = exp(x) - log(conj(y)). MIXED, conj on 2nd argument only.
  PURE form exp(conj(x))-log(conj(y)) absent. Matches pysr_stacking.py. Confirmed.

## 4. eml* uniqueness in the EML ecosystem [ESTABLISHED by web, NON-EXHAUSTIVE]
- 6 public EML implementations inspected: cool-japan/oxieml (Rust), oaustegard/eml-sr (Mojo),
  Ninhache/EML-Operator (PyTorch), tomdif/eml-lean (Lean), VA00/SymbolicRegressionPackage
  (Odrzywolek, Mathematica+Python), SketchLagoon/eml-symbolic-regression-research.
  NONE implements conjugation; all use the holomorphic eml(x,y)=exp(x)-ln(y).
- eml* (conjugation) appears only in Anthony's GitHub issue #5 "Companion operator eml* for
  non-holomorphic functions" on VA00's repo, dated ~2026-05-02 (public dated trace).
- [CONJECTURE] eml* is novel as a named operator. NOT proven (no systematic zbMATH/arXiv review).

## 5. SketchLagoon competitor [CONJECTURE]
- Active EML research campaign (failure modes, optimization landscape, depths 2-6, complex128).
  All visible content is HOLOMORPHIC EML. No conjugation visible. Only READMEs seen, not full code,
  so "no anti-holomorphy" is not certified.

## 6. Strategic recentering [REASONING, 7-AI convergent — convergence is not proof]
- The contribution is NOT "discovering anti-holomorphy" (classical) nor operator novelty per se.
- The defensible contribution is FALSE-POSITIVE CONTROL on native-complex data (SPARC lesson
  formalized). The "operator is pre-wired" triviality objection is certain in peer review.

## 7. Multivariable lock — reclassified [REASONING, 3-AI convergent]
- Mixed-by-variable functions f(z1,z2)=g(z1)h(conj z2) are trivial to construct.
  NOT a publishable open problem. Only a structure/operator-algebra theorem would be novel.

## 8. Formal-artifact hole [CONJECTURE, raised by Qwen]
- The judge certifies the FORMULA exactly, not that the formula is the correct one.
  A spurious PySR formula at finite precision could pass. MSE<1e-3 + negative control are the
  parade, possibly insufficient. Candidate fix: numeric ∂f/∂z̄ evaluation on random complex points.

## NEXT ACTION — 2x2 validation test (parameterization check, NOT a discovery)
- Targets: holomorphic z^2 ; anti-holomorphic emlstar(z,z)=exp(z)-log(conj(z)).
- Data: native complex, |Re|,|Im| <= pi-0.05, N=2000.
- Test B toolbox: +,-,*,/,exp,log,sin,cos, eml  (NO eml*, NO my_conj/my_real/my_imag).
- Test A toolbox: same + eml*.
- Each best formula certified by verify_exact.py. PySR marker is indicative only.
- Expectation: Test B on the anti target should FAIL (holomorphic primitives cannot fabricate
  conjugation — theorem). If it passes -> artifact alarm.
