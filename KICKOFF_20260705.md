# KICKOFF eml* — state as of 2026-07-05 (session: delta_CP closure)

READ FIRST: ./registry.sh show (append-only, entries to #036, script counts 37
due to a pre-existing acknowledged #030 duplicate — never clean it).
Then triple-pass history. Roles unchanged (one-hand-on-repo; Grok consultative;
Milo read-only; arbiter = machine + SymPy judge, never AI vote).

## STATE
- Registry at #036. NEUTRINO SECTOR CLOSED AT EVERY LEVEL:
  real observables (FINDINGS_20260623b) + delta_CP (this session, #036).
  delta_CP verdict [HEURISTIC]: eml* DECORATIVE for CP on the Qu-Lu-Ding
  T-prime model. J_full=-8.13e-03, J_holo=+3.02e-02 (cut does NOT kill J);
  NULL J(Re tau=0)=exact 0; sweep ODD in Re(tau) -> CP phase lives in
  q=exp(2 pi i tau), shared identically by eml AND eml*. L1 bounded to
  Dirac sector MD. Structural closure, not budget. DO NOT REOPEN.
- one-way kernel paper PUBLISHED (Zenodo 10.5281/zenodo.21195311, GitHub
  antparis/oneway-kernel d2cebf2). CLOSED. Grok debate closed.
- #035 DEIMOS complex-shear residual = WALL [budget limit], closed on the
  correct object (full complex field). SOM-gold 21M open in principle only.
- Git: commit 5618f96 (local, NOT pushed) seals delta_CP traces;
  IDEAS_REGISTRY.md now git-tracked for the first time.

## QUEUE (next candidates)
1. pysr_stacking.py --report — May process, read-only consultation, cheap.
2. SOM-gold 21M complex shear — open in principle, costly.
3. (nothing else pending in the neutrino/lensing sectors — both closed)

## OPEN DEBTS (traced, untouched)
- judge_v2.py modified-uncommitted (likely H2 patch #033) — priority git hygiene.
- full_chi2_test.py modified-uncommitted + stale hardcoded VERDICT lines
  ("anti NECESSARY", false) in it and ablation_g1*.py — dated backup fix.
- ~200 untracked files (FINDINGS since 06-26, awake scripts, registry.sh) —
  dedicated git-hygiene session.
- Browser cleanup: DELETE the stale "REPRISE 02-07" block from the profile
  "about you" field (it describes the awake campaign as ongoing — it is
  published and closed); update userPreferences "#001-#034" -> "#036".
- RESEARCH_LOG line 2026-07-05 delta_CP carries a spurious leading ">>"
  (cosmetic, append-only, left as-is).
- Registry format rule (learned this session): entries must be SINGLE LINES
  matching ^#0NN — markdown blocks are invisible to registry.sh. An inert
  "## #036" block sits above the canonical #036 line; harmless, never remove.

## FIRM DECISIONS (do not reopen without history check)
No contact with Odrzywolek. Registry append-only, "cat >" forbidden on it.
Do not reopen: SPARC / 2D target / neutrinos (all levels) / eml* canonical
MIXTE / DEIMOS (#035).

## MILO ARCHIVE BLOCK (ingest when token quota returns)
SESSION 2026-07-05 — delta_CP closure (labels: neutrino, delta_CP, closure,
heuristic). Decisions: (1) first complete run of cp_dcp_test.py (sha256
09f29ce5, audited line-by-line pre-launch), verdict eml* DECORATIVE for CP
[HEURISTIC]; (2) neutrino sector closed at every level; (3) registry
single-line format rule learned + #030 duplicate acknowledged; (4) registry
brought under git. Traces: FINDINGS_20260705_dcp_jarlskog.md,
cp_dcp_run_20260705.log, RESEARCH_LOG line, registry #036, git 5618f96.
